const storage={get:(k,f)=>JSON.parse(localStorage.getItem(k)||JSON.stringify(f)),set:(k,v)=>localStorage.setItem(k,JSON.stringify(v))};const key=new Date().toISOString().slice(0,10);let data=storage.get('noorDuroodCounts',{date:key,today:0,total:0});if(data.date!==key)data={date:key,today:0,total:data.total||0};const $=id=>document.getElementById(id);function draw(){$('todayCount').textContent=data.today;$('totalCount').textContent=data.total;storage.set('noorDuroodCounts',data)}function notify(message){const t=$('toast');t.textContent=message;t.classList.add('show');setTimeout(()=>t.classList.remove('show'),3000)}$('completeDurood').addEventListener('click',()=>{data.today++;data.total++;draw();$('confirmation').textContent='Alhamdulillah 🤍 Your personal Durood count has been saved.';$('completeDurood').animate([{transform:'scale(1)'},{transform:'scale(1.035)'},{transform:'scale(1)'}],{duration:280})});$('resetToday').addEventListener('click',()=>{data.today=0;draw();notify('Today’s personal count has been reset.')});$('menu').addEventListener('click',()=>{const n=$('nav');n.classList.toggle('open');$('menu').setAttribute('aria-expanded',n.classList.contains('open'))});document.querySelectorAll('.nav a').forEach(a=>a.addEventListener('click',()=>$('nav').classList.remove('open')));$('languageSelect').addEventListener('change',e=>{if(e.target.value==='hi')window.location.href='hi/'});$('reminderForm').addEventListener('submit',e=>{e.preventDefault();const choice=new FormData(e.currentTarget).get('reminder');$('reminderStatus').textContent=choice?`${choice} reminder preference saved on this device.`:'Please choose a time first.';if(choice){storage.set('noorDuroodReminder',choice);if('Notification'in window&&Notification.permission==='default')Notification.requestPermission()}});try{const now=new Date();$('gregorianDate').textContent=new Intl.DateTimeFormat(undefined,{dateStyle:'full'}).format(now);$('hijriDate').textContent=new Intl.DateTimeFormat('en-u-ca-islamic',{day:'numeric',month:'long',year:'numeric',weekday:'long'}).format(now)}catch{$('hijriDate').textContent='Hijri date unavailable on this browser'}draw();
