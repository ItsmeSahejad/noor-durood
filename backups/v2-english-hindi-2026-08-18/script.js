const store={get:(k,f)=>JSON.parse(localStorage.getItem(k)||JSON.stringify(f)),set:(k,v)=>localStorage.setItem(k,JSON.stringify(v))};const todayKey=new Date().toISOString().slice(0,10);let counts=store.get('noorDuroodCounts',{date:todayKey,today:0,total:0});if(counts.date!==todayKey)counts={date:todayKey,today:0,total:counts.total||0};const $=id=>document.getElementById(id);function renderCounts(){$('todayCount').textContent=counts.today;$('totalCount').textContent=counts.total;store.set('noorDuroodCounts',counts)}function toast(text){const t=$('toast');t.textContent=text;t.classList.add('show');setTimeout(()=>t.classList.remove('show'),3200)}$('completeDurood').addEventListener('click',()=>{counts.today++;counts.total++;renderCounts();$('confirmation').textContent='Alhamdulillah 🤍 તમારો Durood count સાચવાયો છે.';$('completeDurood').animate([{transform:'scale(1)'},{transform:'scale(1.04)'},{transform:'scale(1)'}],{duration:300})});$('resetToday').addEventListener('click',()=>{counts.today=0;renderCounts();toast('Today’s personal count has been reset.')});$('menu').addEventListener('click',()=>{const n=document.querySelector('.nav');n.classList.toggle('open');$('menu').setAttribute('aria-expanded',n.classList.contains('open'))});document.querySelectorAll('.nav a').forEach(a=>a.addEventListener('click',()=>document.querySelector('.nav').classList.remove('open')));$('reminderForm').addEventListener('submit',e=>{e.preventDefault();const s=new FormData(e.currentTarget).get('reminder');if(!s){$('reminderStatus').textContent='Please choose a gentle time first.';return}store.set('noorDuroodReminder',s);$('reminderStatus').textContent=`${s} reminder saved on this device.`;if('Notification'in window&&Notification.permission==='default')Notification.requestPermission()});$('languageSelect').addEventListener('change',e=>{const v=e.target.value;document.documentElement.dir=['ar','ur','fa'].includes(v)?'rtl':'ltr';document.documentElement.lang=v;toast(v==='en'?'English selected.':'Language preference saved for this session.');sessionStorage.setItem('noorLanguage',v)});const savedLanguage=sessionStorage.getItem('noorLanguage');if(savedLanguage){$('languageSelect').value=savedLanguage;$('languageSelect').dispatchEvent(new Event('change'))}try{const n=new Date();$('gregorianDate').textContent=new Intl.DateTimeFormat(undefined,{dateStyle:'full'}).format(n);$('hijriDate').textContent=new Intl.DateTimeFormat('en-u-ca-islamic',{day:'numeric',month:'long',year:'numeric',weekday:'long'}).format(n)}catch{$('hijriDate').textContent='Hijri date unavailable on this browser'}renderCounts();
