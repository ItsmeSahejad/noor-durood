# Blog architecture

Add only original, human-reviewed articles with verified references. Each future article should include a clean URL, breadcrumb, title, description, category, related articles and valid Article structured data. The sample card links to the reusable article template in `pages/`.
