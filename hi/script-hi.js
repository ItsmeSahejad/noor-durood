const storage={get:(k,f)=>JSON.parse(localStorage.getItem(k)||JSON.stringify(f)),set:(k,v)=>localStorage.setItem(k,JSON.stringify(v))};const key=new Date().toISOString().slice(0,10);let data=storage.get('noorDuroodCounts',{date:key,today:0,total:0});if(data.date!==key)data={date:key,today:0,total:data.total||0};const $=id=>document.getElementById(id);function draw(){$('todayCount').textContent=data.today;$('totalCount').textContent=data.total;storage.set('noorDuroodCounts',data)}function notify(message){const t=$('toast');t.textContent=message;t.classList.add('show');setTimeout(()=>t.classList.remove('show'),3000)}$('completeDurood').addEventListener('click',()=>{data.today++;data.total++;draw();$('confirmation').textContent='अल्हम्दुलिल्लाह 🤍 आपकी निजी दुरूद गिनती सहेज ली गई है।'});$('resetToday').addEventListener('click',()=>{data.today=0;draw();notify('आज की निजी गिनती रीसेट कर दी गई है।')});$('menu').addEventListener('click',()=>{const n=$('nav');n.classList.toggle('open');$('menu').setAttribute('aria-expanded',n.classList.contains('open'))});document.querySelectorAll('.nav a').forEach(a=>a.addEventListener('click',()=>$('nav').classList.remove('open')));$('languageSelect').addEventListener('change',e=>{if(e.target.value==='en')window.location.href='../'});$('reminderForm').addEventListener('submit',e=>{e.preventDefault();const choice=new FormData(e.currentTarget).get('reminder');$('reminderStatus').textContent=choice?`${choice} की याद इस डिवाइस पर सहेज ली गई है।`:'कृपया पहले समय चुनें।';if(choice)storage.set('noorDuroodReminder',choice)});try{const now=new Date();$('gregorianDate').textContent=new Intl.DateTimeFormat('hi-IN',{dateStyle:'full'}).format(now);$('hijriDate').textContent=new Intl.DateTimeFormat('hi-IN-u-ca-islamic',{day:'numeric',month:'long',year:'numeric',weekday:'long'}).format(now)}catch{$('hijriDate').textContent='इस ब्राउज़र में हिजरी तारीख़ उपलब्ध नहीं है'}draw();
